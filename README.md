**Topics Learnt** 
* Rivision of Libraries `Numpy`,`Pandas`,`Matplotlib` and `PyTorch`
* `Neural Networks` and `Deep Learning`
* Avoiding `Overfitting` by `Dropouts`, `Regularization` and `Batch Normalization`
* `Transfer Learning` and `Fine Tuning`
* `Convolutional Neural Networks`
* Various Convolutional Neural Network Architectures
* Introduction to `Encoder-Dcoder` Models, `Attention` , `Self Attention` and `Transformer Architecture`

**Mid Term Assignemnt** 
* Building a `Skin Lesion Classification` Model using `CNN`
* Fine Tuning Pre-Trained `ResNet` Models to perform the same task
* Comparing the performance using `Precision`,`Recall`, `F1 Score`, `Confusion Matrices` and `PR Curves`